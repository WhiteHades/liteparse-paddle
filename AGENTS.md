## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues in `WhiteHades/liteparse-paddle`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the canonical mattpocock/skills triage labels unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
