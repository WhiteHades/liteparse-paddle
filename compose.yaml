services:
  liteparse-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: liteparse-server
    ports:
      - "${LP_PORT:-5000}:5000"
    environment:
      PADDLE_OCR_URL: http://paddle-ocr:8829/ocr
    depends_on:
      paddle-ocr:
        condition: service_healthy
        required: false
      paddle-ocr-gpu:
        condition: service_healthy
        required: false
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/health')"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  paddle-ocr:
    profiles: ["cpu", "default"]
    build:
      context: ./python
      dockerfile: Dockerfile
      args:
        PADDLE_INDEX: ${PADDLE_INDEX:-https://www.paddlepaddle.org.cn/packages/stable/cpu/}
    container_name: paddle-ocr
    ports:
      - "8829:8829"
    environment:
      PADDLE_OCR_TIER: ${PADDLE_OCR_TIER:-medium}
    volumes:
      - paddle-ocr-models:/root/.paddlex
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8829/health')"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 300s
    restart: unless-stopped

  # GPU acceleration: set LP_GPU=1 in your shell or .env, then:
  #   LP_GPU=1 PADDLE_INDEX=https://www.paddlepaddle.org.cn/packages/stable/cuda12/ \
  #     docker compose --profile gpu up -d --build
  # Requires NVIDIA Container Toolkit on the host. The `gpu` profile only
  # brings up the paddle-ocr service with GPU access; liteparse-server stays
  # on the CPU (its workload is cheap).
  #
  # `network_mode` is intentionally left as the default compose network so the
  # container is reachable on the same Docker network as liteparse-server. The
  # `aliases` below make the GPU service addressable as `paddle-ocr` so the
  # server's PADDLE_OCR_URL=http://paddle-ocr:8829/ocr resolves correctly
  # whether the CPU or GPU variant is running. Do not start both profiles
  # at the same time — they both bind host port 8829.
  paddle-ocr-gpu:
    profiles: ["gpu"]
    build:
      context: ./python
      dockerfile: Dockerfile
      args:
        PADDLE_INDEX: ${PADDLE_INDEX:-https://www.paddlepaddle.org.cn/packages/stable/cuda12/}
    container_name: paddle-ocr-gpu
    ports:
      - "8829:8829"
    environment:
      PADDLE_OCR_TIER: ${PADDLE_OCR_TIER:-medium}
    volumes:
      - paddle-ocr-models:/root/.paddlex
    networks:
      default:
        aliases:
          - paddle-ocr
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8829/health')"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 300s
    restart: unless-stopped

volumes:
  paddle-ocr-models:
